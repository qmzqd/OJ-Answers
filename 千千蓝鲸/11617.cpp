#include <bits/stdc++.h>
using namespace std;
#define _for(i, a, b) for (int i = a; i <= b; i++)
int a, q[114514], ans, hown[114514], n;
int main()
{
    cin >> a;
    for (int i = 0; i < a; i++)
    {
        cin >> q[i];
    }
    cin >> n;
    for (int j = 0; j < n; j++)
    {
        cin >> hown[j];
    }
    for (int i = 0; i < a * n; i++)
    {
        if (a)
    }
    return 0;
}
